# 智能食堂菜品数据库改造 — 2026年7月31日-8月1日

## 一、数据库表结构变更

### 1. dishes 表新增 3 列

| 列名 | 含义 | 可选值 | 默认值 | NOT NULL |
|------|------|--------|--------|----------|
| `dish_type` | 菜品类型 | 餐食 / 小吃 / 饮品 / 加购 | 餐食 | ✅ |
| `dish_category` | 菜品分类 | 面食、米饭、炒菜小炒...（共15类） | 其他 | ✅ |
| `meal_period` | 餐次 | 早餐 / 午餐 / 晚餐 / 全天 / 未知 | 全天 | ✅ |

### 2. stalls 表新增 1 列

| 列名 | 含义 | 可选值 | 默认值 |
|------|------|--------|--------|
| `cuisine_type` | 档口餐饮类型 | 正餐 / 饮品 / 麻辣烫 / 汉堡 / 烧烤小吃类 / 超市 | '' |

## 二、菜品分类方案

### dish_type（菜品类型）

| 类型 | 数量 |
|------|------|
| 餐食 | 2,036 |
| 小吃 | 49 |
| 甜点 | 1 |

### dish_category（菜品分类，15类）

| 分类 | 数量 | 说明 |
|------|------|------|
| 面食 | 745 | 面条/米粉/米线类 |
| 米饭 | 332 | 盖饭/炒饭/拼饭/套餐自选 |
| 炒菜小炒 | 309 | 各类小炒/热菜/豆腐素菜/蛋类 |
| 面点饼类 | 190 | 包子/馒头/烧饼/煎饼/馍/卷/糕 |
| 砂锅类 | 144 | 砂锅/干锅/炖菜煲/黄焖鸡 |
| 早点类 | 132 | 粥/肠粉/油条/豆腐脑/亿笼香/杭州小笼包 |
| 饺子 | 82 | 水饺/蒸饺/煎饺/馄饨/锅贴 |
| 烧烤小吃类 | 46 | 烧烤/炸鸡/小吃零食/炸物 |
| 汉堡 | 31 | 汉堡/鸡腿堡 |
| 汤品炖品 | 24 | 汤类/炖品 |
| 麻辣烫 | 23 | 麻辣烫/麻辣香锅/冒菜 |
| 简餐类 | 18 | 沙拉/轻食/悦轻荷 |
| 凉拌冷菜 | 8 | 凉拌/捞汁/擂椒 |
| 烘焙甜点 | 1 | 烘焙类 |
| 火锅 | 1 | 火锅 |

### meal_period（餐次）

全部设置为「全天」。

### stalls.cuisine_type（档口餐饮类型，6类）

| 类型 | 档口数 |
|------|--------|
| 正餐 | 59 |
| 饮品 | 19 |
| 麻辣烫 | 7 |
| 汉堡 | 6 |
| 烧烤小吃类 | 5 |
| 超市 | 4 |

## 三、服务端代码变更

### server/database.js
- `CREATE TABLE dishes`：新增 `dish_type` / `dish_category` / `meal_period` 三列
- `CREATE TABLE stalls`：新增 `cuisine_type` 列
- ALTER TABLE 迁移：新增 4 条兼容语句
- `rowToDish()`：新增 `dishType` / `dishCategory` / `mealPeriod` 字段
- `rowToStall()`：新增 `cuisineType` 字段
- 种子数据 INSERT：新增 3 列默认值

### server/app.js
- Admin dishes 配置：`columns` 和 `writable` 数组新增 3 列
- POST/PATCH 接口：新增 `dishType` / `dishCategory` / `mealPeriod` 校验和持久化

### server/migrations/
- 新增 `029_dish_business_fields.sql`（PostgreSQL 迁移）

## 四、分类逻辑说明

菜品分类采用**末尾字优先**策略：
1. 先剥离括号后缀（如"(2蛋)"、"(大份)"）
2. 判断最后一个字确定大类：面→面食，饭→米饭，粉→面食，包→面点饼类，饺→饺子，煲→砂锅类，汤→汤品炖品...
3. 对于"盖浇X"模式，按末尾字二次判断（盖浇面→面食，盖浇饭→米饭）
4. 特定档口级覆盖（临榆炸鸡腿→小吃/炸鸡，汉堡档口→汉堡）
5. 关键字匹配覆盖剩余情况

## 五、脚本文件

| 文件 | 用途 |
|------|------|
| `scripts/classify-dishes.mjs` | 菜品自动分类脚本 |
| `scripts/view-stalls-dishes.mjs` | 生成档口-菜品关联HTML视图 |
| `scripts/generate-excel.mjs` | 生成三级分类Excel表 |
| `stalls-dishes-view.html` | 交互式档口菜品浏览页面 |

## 六、输出文件

- `菜品三级分类表_modified.xlsx`：按参考格式生成的 2086 条菜品三级分类表
