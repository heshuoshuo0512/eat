# 智慧食堂项目 - 记忆文件

## 用户偏好
- 语言：中文
- 沟通风格：简洁直接

## 项目上下文
- 项目路径：D:/Projects/智慧食堂
- 当前阶段：规划/文档阶段（尚无代码）
- 已有文档：技术规划文档、PPT、需求调查文档、前端后端 agent roadmap

- **关键**：agent/models.yml 必须与根目录 models.yml 保持一致！系统读的是 agent/models.yml，不是根目录的。
- **thinking 字段格式**：DeepSeek 模型的 `thinking` 字段必须同时有 `mode: effort` 和 `efforts: [high]`（efforts 必须是数组），否则 Schema error 会导致整个配置文件解析失败
- 需要修改两个文件：根目录 models.yml 和 agent/models.yml，都要同步
- 可用模型：
  - sui-xiang/gpt-5.5（默认）
  - sui-xiang/gpt-5.4
  - sui-xiang/gpt-5.4-mini
  - sui-xiang/gpt-5.3-codex-spark
  - sui-xiang/gpt-5.2
  - deepseek/deepseek-chat
- agent/config.yml 中 modelRoles.default 已设为 sui-xiang/gpt-5.5
- 配置时间：2026-07-05

## 已知问题与解决
- **每次重启默认 claude-opus**：modelRoles.default 只是角色别名，不控制初始模型。初始模型存在 agent.db 中，需要用 `/model` 命令选 sui-xiang/gpt-5.5 设为默认，或启动时 `omp --model sui-xiang/gpt-5.5`

## 历史记录
- 2026-07-05：首次会话，用户询问 GPT-5.5 模型，项目暂无代码
- 2026-07-05：修复 models.yml 位置问题，从 agent/ 移至 .omp/ 根目录
- 2026-07-05：修复 agent/models.yml 为空文件导致 Schema error，已同步根目录内容
- 2026-07-05：修复 DeepSeek thinking 字段格式错误（mode: effort → efforts: high），同步修改根目录和 agent/models.yml，解决所有模型不可用问题
