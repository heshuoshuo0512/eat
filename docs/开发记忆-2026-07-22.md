# 智慧食堂开发记忆 - 2026-07-22

## 今天完成

1. 审核并确认管理员分支适配已经合并到 `main`，保留管理员响应式说明，不覆盖主分支已有学生端、Agent、检索和后端能力。
2. 审核 `feature/sql`，没有整支合并，因为其中会删除主分支已有的档口父子层级、`campus_posts`、审计元数据和 RAG 1536 维设计。
3. 选择性叠合数据库设计：
   - `dishes.regional_taste`
   - `dishes.allergens_json`
   - `health_profiles.allergies_json`
4. 同步 SQLite 建表/旧库升级、PostgreSQL 基线和 `010_region_allergen_contract.sql` 增量迁移。
5. 健康档案保存后会持久化 `allergies`；普通推荐和 Agent 推荐都会按食材及过敏原硬过滤。
6. 管理员菜品保存支持 `regionalTaste`，并在 `rowToDish`、检索候选中返回该字段。
7. 完善 `openapi/smart-canteen.yaml`：登录注册、健康档案、评价、帖子、管理员审核、菜品区域/过敏原字段和请求体契约。
8. 新增交接文档：
   - `docs/后端接口-Apifox新增接口清单-2026-07-22.md`
   - `docs/后端接口-现有未文档化接口补充-2026-07-22.md`
   - `docs/数据库叠合与最终设计-2026-07-22.md`

## 验证结果

- `npm.cmd run build` 通过。
- OpenAPI YAML 解析通过。
- 本轮数据库、健康档案和推荐核心测试 `55/55` 通过。
- 完整测试 `556/572` 通过；剩余 16 项是已有 Agent 下单确认、供应状态和今日菜单基线问题，本轮没有修改这些模块。
- `git diff --cached --check` 通过。

## Git 状态

- 当前分支：`main`
- 最新提交：`f1dcbb9 feat: reconcile feature sql schema and document APIs`
- 管理员合并提交：`0af3ca4 merge: review admin responsive adaptation`
- 当前 `main` 比 `origin/main` 超前 3 个提交，尚未推送。
- `.idea/`、`.codex-worktrees/` 和旧的未提交工作记录已保留，不能清理或硬覆盖。

## 明天继续

1. 确认是否将 `main` 推送到远端。
2. 推送后再部署 `101.34.216.33`，部署前检查服务器 stash 和未提交改动。
3. 服务器执行数据库迁移并验证 `/api/health`、登录、首页、健康档案、推荐、评价、帖子和静态资源。
4. 单独排查已有的 Agent 确认流程及供应状态测试，不要与本次数据库叠合混在一起。
