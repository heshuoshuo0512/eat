# 智慧食堂上线前自查与今日工作汇报（2026-08-08）

## 今日完成

1. Web 和小程序菜品分页继续统一为每页 20 条。
2. 新增当前页缓存、下一页预取、并发请求合并和筛选条件隔离，预取不会覆盖当前页面。
3. Web 增加菜品骨架屏、翻页加载遮罩、中文加载页码提示和重试保留状态。
4. 小程序增加菜品骨架屏、中文加载页码提示、顶部锚点滚动和下一页预取。
5. 指定菜品分类迁移已生成：
   - 蒜蓉粉丝娃娃菜 -> 家常热菜；
   - 红油抄手（汤/拌） -> 面食粉类；
   - 沙拉脆皮鸡饭、沙拉烤肉拌饭 -> 米饭套餐；
   - 酸汤肉片 -> 家常热菜；
   - 刀削面保持面食粉类。
6. 新增 `033` 指定分类迁移和受保护回滚脚本，使用稳定菜品 ID、事务校验和分类审计表。
7. 14 份单店连锁菜单完成清洗，`华莱士.md` 已从来源循环和发布批次中删除：
   - 824 条已批准发布项；
   - 122 条明确排除项；
   - 282 条待核验项；
   - 共享菜单记录 130 条；
   - `连锁店菜单合集.md` 仅作为重复核对源。
8. 新增 `034` 连锁菜单发布审计迁移、稳定 ID 发布清单、幂等导入器和回滚脚本；发布摘要哈希为 `1569d719a445da0b3b5bb8e6eaec591f6870e85380c2204dfb3fc92c9fed3449`。
9. 824 条已批准菜单已在生产 PostgreSQL 单事务导入，创建 4 个缺失档口：茶百道（广缘店）、肯德基（广缘店）、塔斯汀中国汉堡（大榕树餐厅）、益禾堂（广缘店）。
10. 代码提交 `eb4a0b2` 完成菜单发布，`c232038` 修正导入器幂等重试并已推送 `main`；服务器当前目录为 `/opt/smart-canteen/app -> app-c232038`。
11. `033` 已在生产 PostgreSQL 事务中执行，6 条目标记录全部完成分类审计并通过分类、价格和稳定 ID 校验。
12. 菜品检索索引已重建：默认租户文档 3295 条，其中菜品 3137、档口 142、食堂 14；全局知识文档 510 条，失败数为 0；向量模式继续关闭，使用词法检索。
13. 线上搜索冒烟通过：华莱士查询结果为 0；茶百道饮品查询返回 44 条，结果携带真实档口、食堂、价格和来源批次信息。

## 已完成基线

- `031_account_public_profile.sql` 和 `032_community_lifecycle.sql` 已存在并曾部署。
- 账号资料门槛、帖子/评价/评论基础接口、自动审核核心规则和管理员举报接口已存在。
- 真实目录检索、Redis 完整响应缓存和 Redis 就绪检查已存在。
- 当前服务器版本：`c232038`；菜单发布前运行基线为 `bd8e992`，更早回滚基线为 `7642c06`。
- `http://82.156.12.26/api/health/live` 返回 200。
- `http://82.156.12.26/api/health/ready` 返回 200，数据库正常，Redis 为分布式后端且未降级。
- 生产备份已完成并验证恢复：`/var/backups/smart-canteen/20260808T055233Z`，恢复验证包含 53 张表和 3 个用户。
- 发布前配置与版本快照已由服务器备份工具保存；当前 API、Web、PostgreSQL、Redis、MinIO 均健康。

## 仍需完善

- Web 和小程序发帖、完善资料后恢复草稿、图片重试、评论审核结果展示仍需真实登录流程验收。
- 自动审核后端规则已存在，但拒绝原因、规则版本和评论状态还需要在两端逐项验收。
- AI 菜品介绍当前已批准 `2645/2715` 条，剩余 70 条只能继续做差异审计，不能伪造补齐。
- 当前服务器检索索引使用词法模式，向量嵌入未开启；这不是基础菜品检索阻塞项。
- `stueat.com` 仍在备案，服务器 TCP 443 当前无法连接，正式 HTTPS 证书和微信合法域名尚未完成。
- 小程序生产域名、真机生产网络验收和正式提交审核不能在备案完成前宣称完成。

## 测试结果

- Web 生产构建：已通过。
- 小程序开发联调构建：已通过，使用 `ALLOW_INSECURE_MINIAPP_BUILD=1` 连接当前 IP；正式构建必须改用 HTTPS 域名。
- 指定分类迁移静态审计：已通过。
- 菜品响应缓存测试：已通过。
- 原有分类迁移和小程序检索 UI 合约测试：已通过。
- 全仓测试：已通过（本次 store 合约修正、加载反馈和缓存改动均包含在验证范围内）。
- 线上冒烟：API live/ready、七类分类 API、指定分类搜索、连锁菜单搜索、API/Web 容器健康均已通过。

## 发布边界

- 本次代码、迁移、审计报告和发布清单已提交、推送并部署；生产写入前备份和恢复验证已完成。
- `033` 分类迁移已执行；回滚前必须停止 API、恢复分类 SQL、重建检索索引并重新做 ready 冒烟。
- `034` 连锁菜单批次已导入；仅 824 条 `approved` 清单进入学生检索，华莱士和待核验记录未进入批次。
- 备案完成后再配置 DNS、HTTPS 自动续期、HSTS、微信 request/upload/download/connect 合法域名和小程序生产 API 地址。

## 回滚准备

- 代码可回退到菜单发布前版本 `bd8e992`，当前运行版本为 `c232038`。
- 分类可使用 `server/rollbacks/033_targeted_catalog_category_corrections.rollback.sql` 回滚。
- 回滚分类后必须重建菜品检索索引。
- 连锁菜单可使用 `scripts/import-chain-menu-release-postgres.mjs --rollback` 或受控 SQL 回滚；回滚后必须重建菜品检索索引并验证批次商品、档口和学生检索均已清除。
- 社区故障时可独立关闭社区写入开关，不影响基础菜品浏览和检索。
