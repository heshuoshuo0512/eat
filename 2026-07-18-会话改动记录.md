# 本次会话改动记录

**日期：** 2026-07-18

---

## 一、修改的文件

| 文件 | 改动说明 |
|------|----------|
| `src/App.vue` | 登录模板重构：滑动表单布局（form-box + con-box）、`isLogin` ref 替代 `authMode`、新增 `slideToLogin`/`slideToRegister` 方法 |
| `src/styles/main.css` | 新增登录面板全部样式（约80行）、补 CSS 变量 `--primary-light`、修复 `var(--primary-soft)` 颜色冲突 |
| `package-lock.json` | npm install 自动更新 |

## 二、新建的文件

| 文件 | 说明 |
|------|------|
| `login-preview.html` | 静态预览稿，来自桌面 `login-preview(4).html` |
| 桌面 `注册功能接口文档.md` | 注册接口定义、请求/返回示例、处理流程 |
| 桌面 `注册功能数据库文档.md` | users 表和 health_profiles 表结构、写入示例、表关系 |

## 三、关键技术决策

1. **滑动动画**：用 `left` 属性而非 `transform: translateX()`，避免与 `translateY(-50%)` 居中定位冲突
2. **布局尺寸**：面板 680px、表单 420px、最小高度 720px
3. **CSS 变量冲突修复**：`--primary-soft` 在主样式里是透明色 `rgba(31,122,77,0.12)`，预览稿里是绿色 `#5ec17a`，提交按钮和 span 高亮改用硬编码 `#5ec17a`
4. **补了 `--primary-light: #36a367`**，角色切换按钮和提交按钮渐变依赖该变量

## 四、服务状态

| 服务 | 端口 | 状态 |
|------|------|------|
| 后端 API | 8787 | ✅ 运行中 |
| 前端 Vite | 5176 | ✅ 运行中 |

## 五、演示账号

| 身份 | 用户名 | 密码 |
|------|--------|------|
| 学生 | 演示学生 | student123 |
| 管理员 | admin | admin123 |

## 六、待办

无。登录页合并已完成并验证。
